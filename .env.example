SQUARE_ACCESS_TOKEN=
SQUARE_API_BASE=https://connect.squareupsandbox.com
SQUARE_LOCATION_ID=
ANTHROPIC_API_KEY=

# Gmail SMTP (App Password) — stand-in notification method until Twilio SMS clears
GMAIL_USER=
GMAIL_APP_PASSWORD=

# Optional — cron expression for src/scheduler.js. Defaults to every 15 minutes if unset.
CRON_SCHEDULE=